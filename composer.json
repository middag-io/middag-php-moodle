{
  "name": "middag-io/moodle",
  "description": "MIDDAG Moodle adapter — platform bindings, ACL layer, and Moodle-specific implementations for middag-framework",
  "type": "library",
  "license": "Apache-2.0",
  "require": {
    "php": "^8.2",
    "ext-json": "*",
    "firebase/php-jwt": "^7.0",
    "middag-io/framework": ">=0.11.3 <1.0",
    "middag-io/ui": "^1.0",
    "mikehaertl/php-pdftk": "^0.13",
    "nyholm/psr7": "^1.8",
    "psr/simple-cache": "^3.0",
    "symfony/dependency-injection": "^7.0",
    "symfony/event-dispatcher": "^7.0",
    "symfony/http-client": "^7.0",
    "symfony/http-foundation": "^7.0",
    "symfony/routing": "^7.0"
  },
  "require-dev": {
    "friendsofphp/php-cs-fixer": "^3.0",
    "michaelmeneses/moodle-stubs": "5.2.0",
    "phpstan/phpstan": "^2.0",
    "phpunit/phpunit": "^11.0",
    "rector/rector": "^2.0"
  },
  "autoload": {
    "psr-4": {
      "Middag\\Moodle\\": "src/"
    }
  },
  "autoload-dev": {
    "psr-4": {
      "Middag\\Moodle\\Tests\\": "tests/"
    }
  },
  "config": {
    "sort-packages": true
  },
  "minimum-stability": "dev",
  "prefer-stable": true,
  "scripts": {
    "post-install-cmd": "git config core.hooksPath .githooks 2>/dev/null || true",
    "post-update-cmd": "git config core.hooksPath .githooks 2>/dev/null || true",
    "check": [
      "@check:boundaries",
      "@check:stan",
      "@check:style",
      "@check:rector"
    ],
    "check:boundaries": "grep -rln --include='*.php' 'Middag\\\\Core\\\\' src && { echo 'Article-I VIOLATION: OSS adapter src/ imports a non-OSS MIDDAG namespace/package, which is forbidden'; exit 1; } || echo 'boundaries OK: no non-OSS MIDDAG namespace imports in src/'",
    "check:stan": "phpstan analyse --configuration=.phpstan.neon --memory-limit=512M",
    "check:style": "php-cs-fixer fix --dry-run --verbose --config=.php-cs-fixer.dist.php",
    "check:rector": "rector process --dry-run --config=.php-rector.php",
    "fix": [
      "@fix:style",
      "@fix:rector"
    ],
    "fix:style": "php-cs-fixer fix --verbose --show-progress=none --config=.php-cs-fixer.dist.php",
    "fix:rector": "rector process --config=.php-rector.php --no-progress-bar",
    "test": "phpunit"
  }
}
